# Plano: UI de Cadastro de Estoque — Hierarquia Completa

## Objetivo
Substituir a página e o model `LocalizacaoEstoque` existente por uma nova hierarquia de 6 níveis: **Local → Sala → Corredor → Lado (Esq/Dir) → Bloco → Prateleira**. Código auto-gerado no formato `LOCAL-SALA-CORREDOR-LADO-BLOCO-PRATELEIRA`.

## Arquivos a Modificar

### Backend (Django)
| Arquivo | Mudança |
|---------|---------|
| `backend/django_app/suprimentos/models.py` | Reescrever campos do model `LocalizacaoEstoque` |
| `backend/django_app/suprimentos/serializers.py` | Atualizar campos do serializer |
| `backend/django_app/suprimentos/views.py` | Atualizar filtros do viewset |

### Frontend (React)
| Arquivo | Mudança |
|---------|---------|
| `frontend/src/store/useLocalizacaoEstoqueStore.ts` | Nova interface e defaults |
| `frontend/src/pages/admin/LocalizacaoEstoquePage.tsx` | Reescrever formulário e grid |

---

## 1. Backend — Model (`suprimentos/models.py`)

Substituir os campos do model `LocalizacaoEstoque`:

**Campos ATUAIS (remover):**
- `componente` (FK) → REMOVER (posições são slots vazios)
- `fileira` → substituir por `local`
- `lado` → manter (E/D)
- `nivel` → substituir por `sala`
- `bloco` → manter

**Campos NOVOS:**
```python
class LocalizacaoEstoque(TenantModel):
    local = models.CharField("Local", max_length=50)       # Ex: MATRIZ, FILIAL2
    sala = models.CharField("Sala", max_length=20)          # Ex: S01, S02
    corredor = models.CharField("Corredor", max_length=20)  # Ex: C01, C02
    lado = models.CharField("Lado", max_length=1, choices=LadoEstoque.choices)  # E/D
    bloco = models.CharField("Bloco", max_length=10)        # Ex: B01, B02
    prateleira = models.CharField("Prateleira", max_length=10)  # Ex: P01, P02
    codigo = models.CharField("Código", max_length=100)     # Auto-gerado
    capacidade = models.PositiveIntegerField("Capacidade", default=0)
    quantidade = models.PositiveIntegerField("Quantidade Atual", default=0)
    ativo = models.BooleanField("Ativo", default=True)
```

**Geração de código no `save()`:**
```python
def save(self, *args, **kwargs):
    self.codigo = f"{self.local.upper()}-{self.sala.upper()}-{self.corredor.upper()}-{self.lado.upper()}-{self.bloco.upper()}-{self.prateleira.upper()}"
    super().save(*args, **kwargs)
```
Exemplo: `MATRIZ-S01-C01-E-B01-P01`

**Unique constraint:** `UniqueConstraint(fields=["tenant", "codigo"])` (manter)
**Ordering:** `["local", "sala", "corredor", "lado", "bloco", "prateleira"]`

**Migração:** Criar nova migration. Como é dev (não há dados de produção), pode ser `migrate --fake` se necessário.

---

## 2. Backend — Serializer (`suprimentos/serializers.py`)

Simplificar — remover campos `componente_codigo` e `componente_descricao`:
```python
class LocalizacaoEstoqueSerializer(serializers.ModelSerializer):
    class Meta:
        model = LocalizacaoEstoque
        fields = '__all__'
        read_only_fields = ('codigo',)
```

---

## 3. Backend — View (`suprimentos/views.py`)

Atualizar filtros do `LocalizacaoEstoqueViewSet`:
```python
class LocalizacaoEstoqueViewSet(TenantModelViewSet):
    queryset = LocalizacaoEstoque.objects.all()
    serializer_class = LocalizacaoEstoqueSerializer

    def get_queryset(self):
        qs = super().get_queryset()
        # Filtros por cada nível da hierarquia
        local = self.request.query_params.get('local')
        if local: qs = qs.filter(local__iexact=local)
        sala = self.request.query_params.get('sala')
        if sala: qs = qs.filter(sala__iexact=sala)
        corredor = self.request.query_params.get('corredor')
        if corredor: qs = qs.filter(corredor__iexact=corredor)
        lado = self.request.query_params.get('lado')
        if lado: qs = qs.filter(lado__iexact=lado)
        bloco = self.request.query_params.get('bloco')
        if bloco: qs = qs.filter(bloco__iexact=bloco)
        return qs
```

Remover `select_related('componente')`.

---

## 4. Frontend — Store (`useLocalizacaoEstoqueStore.ts`)

Nova interface:
```typescript
export interface LocalizacaoEstoque {
  id?: number;
  local: string;
  sala: string;
  corredor: string;
  lado: string;       // 'E' | 'D'
  bloco: string;
  prateleira: string;
  codigo?: string;
  capacidade: number | string;
  quantidade: number | string;
  ativo: boolean;
}

const DEFAULT: LocalizacaoEstoque = {
  local: '',
  sala: '',
  corredor: '',
  lado: 'E',
  bloco: '',
  prateleira: '',
  capacidade: 0,
  quantidade: 0,
  ativo: true,
};
```

Manter mesma estrutura de actions: `carregar`, `atualizarCampo`, `selecionar`, `salvar`, `excluir`, `resetForm`.

Validação no `salvar()`: exigir `local`, `sala`, `corredor`, `bloco`, `prateleira`.

---

## 5. Frontend — Page (`LocalizacaoEstoquePage.tsx`)

Manter layout existente (form 1/3 + grid 2/3).

**Formulário — 6 campos hierárquicos:**
```
┌──────────────────────────────┐
│ Local *       │ Sala *       │
│ Corredor *    │ Lado (E/D) * │
│ Bloco *       │ Prateleira * │
│ Capacidade    │ Quantidade   │
│ ☐ Ativo                       │
│ ┌──────────────────────────┐  │
│ │ Código: MATRIZ-S01-C01...│  │  ← Preview do código
│ └──────────────────────────┘  │
│ [Nova]          [Salvar]      │
└──────────────────────────────┘
```

**Preview do código:** Gerado no frontend em tempo real (sem esperar backend), usando a mesma lógica:
```typescript
const codigoPreview = [local, sala, corredor, lado, bloco, prateleira]
  .filter(Boolean)
  .join('-')
  .toUpperCase();
```

**Colunas AG Grid:**
| Coluna | Campo | Largura |
|--------|-------|---------|
| Local | `local` | 120 |
| Sala | `sala` | 80 |
| Corredor | `corredor` | 100 |
| Lado | `lado` | 80 (colorido: E=amarelo, D=azul) |
| Bloco | `bloco` | 80 |
| Prateleira | `prateleira` | 100 |
| Código | `codigo` | 250 (mono, bold, primary color) |
| Qtd | `quantidade` | 70 |
| Cap. | `capacidade` | 70 |
| Ações | botão excluir | 80 |

**Busca:** Filtrar por código, local, sala, corredor, bloco.

---

## 6. Verificação

1. `cd backend/django_app && python manage.py makemigrations suprimentos && python manage.py migrate`
2. `cd frontend && npm run build` (verificar sem erros de TypeScript)
3. Testar CRUD: criar posição, verificar código gerado, editar, excluir
4. Verificar que a sidebar em `/admin/localizacao-estoque` continua acessível
